
const icons = {
    fb : require('./facebook.svg').default.src,
    github : require('./github.svg').default.src,
    twitter : require('./twitter.svg').default.src,
    instagram : require('./instagram.svg').default.src,
    ln : require('./ln.svg').default.src,
}

export default icons;